{#<script>#}
var leadsfactory = {
    html: '{{ include(template_from_string(form.source))|e('js') }}',
    init: function(form_id, data) {
        var $ = jQuery;
        var form = $('#'+form_id);
        $.each(data, function(key, value) {
            $('[name=lffield\\['+key+'\\]]', form).val(value);
        });
        {{ form.script|raw }}
    }
};
{#</script>#}
