<!-- Messages -->
<div class="col-md-12" style="margin-bottom:10px;">
{% for item in messages %}
<div class="col-md-12">
    <div class="notifications widget-body {% if item.type == 2 %}pink{% else %}yellow{% endif %}">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <p>{% if item.title %}<b>{{ item.title }} :&nbsp;</b>{% endif %}{{ item.message }}</p>
    </div>
</div>
{% endfor %}
</div>