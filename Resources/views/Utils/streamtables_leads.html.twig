[
{% for item in items %}
    {
        "identifiant": "{{ item.id }}",
        "firstname": "{{ item.firstname }}",
        "lastname": "{{ item.lastname }}",
        "status": "{{ item.status }}",
        "action": ""
    }{% if not loop.last %},{% endif %}
{% endfor %}
{}]